<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>
    <groupId>com.meirongdev</groupId>
    <artifactId>sse-vs-ws-jetty</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>sse-vs-ws-jetty</name>
    <description>The servlet module's own source, on Jetty instead of Tomcat.</description>

    <properties><java.version>25</java.version></properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <!-- The one substitution this module makes. Everything above and below it is unchanged. -->
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-tomcat</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jetty</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-tomcat</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
    </dependencies>

    <build>
        <!--
          ⚠ Points at the SERVLET MODULE'S OWN SOURCE, not a copy.
          A copy would drift, and the whole value of this module is that the Java is provably identical:
          the only difference between it and the Tomcat build is which container the starter pulls in.
        -->
        <sourceDirectory>${project.basedir}/../server/src/main/java</sourceDirectory>
        <resources>
            <resource><directory>${project.basedir}/../server/src/main/resources</directory></resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <!--
                      TomcatTuning imports Connector, Http2Protocol and TomcatConnectorCustomizer, none of
                      which exist on this classpath. Excluding it is not a workaround but a statement of the
                      comparison's terms: THIS BUILD HAS NO CONTAINER TUNING, so it is compared against the
                      Tomcat build's UNTUNED baseline (111.8 KB/connection), both stacks at their defaults.
                    -->
                    <excludes><exclude>**/config/TomcatTuning.java</exclude></excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
